# 快速开始：使用模板生成项目骨架

## 5 分钟快速入门

### 第 1 步：启动应用

```bash
# 进入项目目录
cd dexcode

# 启动应用（包含 Web 服务和 Runtime）
npm run dev
```

应用会在 http://localhost:3000 启动。

### 第 2 步：打开应用并生成项目

1. 在浏览器打开应用
2. 找到右侧"工作区文件树"面板的"新建"按钮
3. 点击"新建" → 选择"📦 生成项目模板"
4. 在模板选择对话框中选择你想要的项目模板：
   - **前端项目**: Vite + React, Create React App
   - **后端项目**: Express API, Spring Boot
   - **全栈项目**: Next.js 应用
5. 输入项目名称（例如：`my-awesome-app`）
6. 点击"创建"

### 第 3 步：等待项目生成

系统会流式生成项目文件，你可以在对话区看到实时进度：

```
正在生成项目…
✓ 已生成 1 个文件…
✓ 已生成 2 个文件…
...
✅ 项目骨架生成完成！
```

### 第 4 步：开始开发

生成完成后，你可以：

- 在文件树中浏览生成的项目结构
- 点击文件查看和编辑代码
- 继续输入 Agent 命令来修改或添加功能

## 可用的项目模板

### 前端项目

#### Vite + React + TypeScript

```
项目特点：
- 快速的开发服务器和热更新
- 完整的 TypeScript 支持
- ESLint 代码检查
- 生产优化构建

快速开始：
npm install
npm run dev
```

#### Create React App

```
项目特点：
- 零配置的 React 项目
- 内置 Jest 测试框架
- 自动刷新
- 生产构建优化

快速开始：
npm install
npm start
```

### 后端项目

#### Express API 服务

```
项目特点：
- 轻量级 REST API 框架
- 完整的 TypeScript 支持
- 错误处理中间件
- 开发热更新（使用 tsx-watch）

快速开始：
npm install
npm run dev
```

#### Spring Boot 应用

```
项目特点：
- 完整的 Java Spring 框架
- Maven 依赖管理
- 内置 Tomcat 服务器
- 配置文件支持（YAML）

快速开始：
mvn clean package
mvn spring-boot:run
```

### 全栈项目

#### Next.js 应用

```
项目特点：
- 全栈 JavaScript 框架
- App Router 路由系统
- 服务端和客户端渲染
- 内置 API Routes

快速开始：
npm install
npm run dev
```

## 常见任务

### 任务 1：快速生成一个 React 应用

1. 点击"新建" → "📦 生成项目模板"
2. 选择 "Vite + React + TypeScript"
3. 输入项目名称：`my-react-app`
4. 等待生成完成
5. 打开 `src/App.tsx` 开始编辑

### 任务 2：创建一个 Express API 服务

1. 点击"新建" → "📦 生成项目模板"
2. 选择 "Express API 服务"
3. 输入项目名称：`my-api-server`
4. 打开 `src/routes/index.ts` 添加自己的路由

### 任务 3：启动 Next.js 全栈应用

1. 点击"新建" → "📦 生成项目模板"
2. 选择 "Next.js 应用"
3. 输入项目名称：`my-fullstack-app`
4. 生成后编辑 `app/page.tsx` 自定义首页

## 使用 Agent 命令修改项目

生成项目后，你可以继续给 Agent 输入命令来修改项目：

### 示例 1：添加新的 React 组件

```
在 src 目录下创建一个名为 Button 的新 React 组件，包含点击事件处理
```

### 示例 2：为 Express 添加数据库连接

```
为 Express 项目添加 MongoDB 连接，包括连接配置和错误处理
```

### 示例 3：改进项目结构

```
重新组织项目结构，将代码分为 components, pages, utils, styles 等目录
```

## API 调用示例

### 使用 curl 生成项目

```bash
# 生成 Vite + React 项目
curl -X POST http://localhost:3000/api/scaffold/generate \
  -H "Content-Type: application/json" \
  -d '{
    "projectName": "my-project",
    "templateId": "vite-react-ts"
  }' \
  --no-buffer
```

### 获取所有可用模板

```bash
curl http://localhost:3000/api/templates | jq
```

### 按类别获取模板

```bash
# 获取所有前端项目模板
curl http://localhost:3000/api/templates/category/frontend | jq

# 获取所有后端项目模板
curl http://localhost:3000/api/templates/category/backend | jq
```

## 项目生成后的文件结构

### Vite + React 项目

```
my-awesome-app/
├── src/
│   ├── App.tsx          # 主应用组件
│   ├── App.css          # 组件样式
│   ├── main.tsx         # 应用入口
│   └── index.css        # 全局样式
├── package.json         # 依赖和脚本
├── index.html          # HTML 模板
├── vite.config.ts      # Vite 配置
├── tsconfig.json       # TypeScript 配置
└── .gitignore          # Git 忽略配置
```

### Express API 项目

```
my-api-server/
├── src/
│   ├── index.ts             # 应用入口
│   ├── routes/
│   │   └── index.ts         # API 路由
│   └── middleware/
│       └── errorHandler.ts  # 错误处理
├── package.json            # 依赖和脚本
├── tsconfig.json          # TypeScript 配置
├── .env.example           # 环境变量示例
└── .gitignore             # Git 忽略配置
```

## 故障排查

### 问题：生成按钮找不到

**解决方案**：检查左侧"工作区文件树"面板，找到顶部的"新建"按钮，点击后会出现下拉菜单。

### 问题：模板选择对话框无法打开

**解决方案**：

1. 刷新页面
2. 检查浏览器控制台是否有错误
3. 确保应用服务器正在运行

### 问题：文件没有生成到工作区

**解决方案**：

1. 检查工作区目录权限
2. 查看浏览器控制台的错误信息
3. 尝试使用不同的项目名称

## 下一步

- 查看 [模板与骨架生成系统说明](模板与骨架生成系统说明.md) 了解更详细的信息
- 学习如何 [自定义模板](模板与骨架生成系统说明.md#扩展模板系统)
- 查看 [架构设计文档](架构.md) 了解系统设计

## 支持的项目类型总结

| 模板ID        | 名称                      | 类别 | 语言       | 框架            | 文件数 |
| ------------- | ------------------------- | ---- | ---------- | --------------- | ------ |
| vite-react-ts | Vite + React + TypeScript | 前端 | TypeScript | React 18        | 11     |
| react-app     | Create React App          | 前端 | JavaScript | React 18        | 8      |
| next-app      | Next.js 应用              | 全栈 | TypeScript | Next.js 14      | 8      |
| express-api   | Express API 服务          | 后端 | TypeScript | Express 4.18    | 7      |
| spring-boot   | Spring Boot 应用          | 后端 | Java       | Spring Boot 3.2 | 5      |

## 获取帮助

如果在使用过程中遇到问题：

1. 查看浏览器开发者工具（F12）中的控制台错误信息
2. 查看应用后台的日志输出
3. 尝试重新启动应用
4. 阅读详细文档获取更多信息
