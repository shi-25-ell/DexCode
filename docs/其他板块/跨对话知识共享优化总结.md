# 跨对话知识共享优化总结

## 优化目标

根据系统演进设计文档 3.2“跨对话知识共享”的要求，本次在 `Context-Engineering` 分支上补齐项目级知识库能力，让 Agent 可以在不同会话之间共享和复用项目经验。

## 已实现能力

### 1. 项目级知识库存储

修改文件：`packages/session-store/index.ts`

- 使用 `workspaces/<projectId>/DEXCODE.md` 作为项目级知识库。
- 新增默认 Markdown 模板，覆盖编码规范、技术决策、常见问题、用户偏好、Agent 任务经验。
- 新增 `getProjectKnowledge()`，返回内容、文件路径、是否已存在和默认模板。
- 新增 `writeProjectKnowledge()`，支持用户完整编辑并保存 Markdown。
- 新增 `appendProjectKnowledge()`，支持将任务经验追加到指定知识分区。

### 2. 项目知识 API

修改文件：`apps/runtime/server.ts`

新增接口：

- `GET /api/project-knowledge`：读取项目知识；文件不存在时返回默认模板。
- `PUT /api/project-knowledge`：覆盖保存项目知识。
- `POST /api/project-knowledge/append`：追加一条经验到指定分区，默认写入 `Agent 任务经验`。

这些接口让项目知识可以被 UI、Agent 流程和未来自动提取机制复用。

### 3. Context Builder 检索集成

修改文件：`packages/context-builder/index.ts`

上一轮上下文工程优化已完成以下集成，本次继续沿用：

- `buildForPrompt(prompt, selectedFile, { projectKnowledge })` 接收项目知识。
- 小于约 2KB 的知识全量注入。
- 较大的 `DEXCODE.md` 会按 Markdown 标题拆分段落。
- 使用 prompt token 匹配相关段落，只把命中的项目知识注入上下文。
- 检索结果进入 `projectKnowledgeSummary`，由 Agent system prompt 注入。

### 4. Agent 任务经验沉淀建议

修改文件：`packages/agent-core/index.ts`

- 任务完成后，根据用户需求、已修改文件和使用工具生成“项目知识建议”。
- 建议文本被追加到任务摘要中，提醒用户哪些经验适合保存为跨会话共享知识。

### 5. Web UI 项目知识入口

修改文件：

- `apps/web/index.html`
- `apps/web/app.ts`
- `apps/web/styles.css`

新增“项目知识”面板：

- 支持查看和编辑 Markdown 格式项目知识。
- 支持保存完整项目知识。
- 支持重载项目知识。
- 支持一键将最近任务摘要保存到 `Agent 任务经验`。
- 保存后的内容会立即刷新到编辑器中，后续会话执行任务时可被检索注入。

## 对设计文档的覆盖情况

| 设计项 | 实现情况 |
|---|---|
| 项目级知识库 | 已实现，存储在 `DEXCODE.md` |
| 手动录入 | 已实现，Web UI 可直接编辑保存 Markdown |
| Agent 自动提取 | 已实现基础闭环：任务摘要生成建议，用户一键保存 |
| 全量注入 | 已实现，小知识库直接注入 |
| 关键词匹配 | 已实现，按 prompt token 检索 Markdown 段落 |
| 向量化存储 | 暂未实现，保留为后续可选项 |
| 工作区面板项目知识 Tab | 已实现为执行摘要区内的“项目知识”卡片 |

## 验证结果

已通过 TypeScript 检查：

```bash
node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit
node node_modules/typescript/bin/tsc -p apps/web/tsconfig.build.json --noEmit
```

## 后续建议

1. 增加项目知识检索单元测试，覆盖小知识库全量注入和大知识库分段检索。
2. 将“保存任务经验”升级为可编辑确认弹窗，让用户在写入前调整措辞。
3. 后续如项目知识超过几十 KB，可引入 embedding/RAG 索引作为二级检索。
